clc; clear all; close all;

lines = readlines("adventofcode.com_2022_day_1_input.txt");

calories = str2double(lines);

highest = 0;
current = 0;
for i = 1:length(calories)
    if isnan(calories(i))

    end
    current = current + calories(i);
end